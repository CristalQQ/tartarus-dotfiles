Чтобы поставить русскую раскладку клавиатуры: 
Узнать модель: setxkbmap -query
localectl set-x11-keymap --no-convert us,ru pc105 "" grp:alt_shift_toggle


Pacman: git lightdm firefox fish starship kitty discord picom rofi neofetch nitrogen gtk2 gtk3 1xappearance maim xclip timeshift thunar vim vscode xdg-user-dirs pavucontrol
yay: lightdm-mini-greeter light spotify nekoray sing-geosite sing-geoip


Установка yay:
1) git clone https://aur.archlinux.org/paru.git
2) makepkg -si


Установка AwesomeWM:
sudo pacman -S --needed base-devel git
git clone https://aur.archlinux.org/awesome-git.git
cd awesome-git
makepkg -fsri


Установить по умолчанию скринлок:
systemctl enable lightdm


fish поумолчанию 
chsh -s /usr/bin/fish

Добавить пользователя в light:
sudo usermod -aG video cristal


Nekoray tun:
sudo setcap cap_net_admin=ep /usr/lib/nekoray/nekobox_core


Spicetify:
yay -S spicetify-cli
sudo chmod a+wr /opt/spotify
sudo chmod a+wr /opt/spotify/Apps -R

spicetify
spicetify backup apply enable-devtools

cd "$(dirname "$(spicetify -c)")/Themes/Dribbblish"
spicetify config current_theme Dribbblish color_scheme gruvbox
spicetify config inject_css 1 replace_colors 1 overwrite_assets 1 inject_theme_js 1
spicetify apply